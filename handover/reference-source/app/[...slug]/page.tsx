import {notFound} from 'next/navigation';
import {ProductList,Product,Conservatories,About,Areas,Gallery,Reviews,Blog,Quote,Legal} from '../site';
import {windows,doors,areas,articles} from '../content';
export async function generateMetadata({params}:{params:Promise<{slug:string[]}>}){const {slug}=await params;const last=slug[slug.length-1];const product=[...windows,...doors].find(x=>x.slug===last);const article=articles.find(x=>x.slug===last);const name=product?.name||article?.title||last.replace(/-/g,' ').replace(/\b\w/g,c=>c.toUpperCase());return {title:name,description:product?.text||article?.intro||`Explore ${name.toLowerCase()} with Splendid Double Glazing in Sidcup and South East London.`}}
export default async function Page({params}:{params:Promise<{slug:string[]}>}){const {slug}=await params;const path=slug.join('/');if(path==='windows'||path==='doors')return <ProductList type={path}/>;if(slug.length===2&&(slug[0]==='windows'||slug[0]==='doors')){const type=slug[0];if(!(type==='windows'?windows:doors).find(x=>x.slug===slug[1]))notFound();return <Product type={type} slug={slug[1]}/>;}if(path==='conservatories'||path==='porches')return <Conservatories porch={path==='porches'}/>;if(path==='about')return <About/>;if(path==='service-areas')return <Areas/>;if(slug.length===1&&path.startsWith('windows-doors-')){const area=path.replace('windows-doors-','');if(!areas.find(x=>x[0]===area))notFound();return <Areas slug={area}/>;}if(path==='gallery')return <Gallery/>;if(path==='reviews')return <Reviews/>;if(path==='blog')return <Blog/>;if(slug.length===2&&slug[0]==='blog'&&articles.find(x=>x.slug===slug[1]))return <Blog slug={slug[1]}/>;if(['free-quote','contact','estimate-calculator'].includes(path))return <Quote contact={path==='contact'} planner={path==='estimate-calculator'}/>;if(['privacy-policy','cookie-policy','terms-of-service'].includes(path))return <Legal type={path}/>;notFound();}
